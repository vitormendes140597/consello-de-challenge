# ai-alert-scorer AGENTS

This file overrides the repository root `AGENTS.md` for everything under `ai-alert-scorer/`.

## Project Scope

- Python agent built with LangChain and LangGraph
- Keep implementation focused on agent orchestration, scoring logic, and prompt/tool boundaries

## Project Rules

- Prefer modular code under `src/ai_alert_scorer/`.
- Keep tests in `tests/`.
- Add project-specific notes here as the agent implementation evolves.

